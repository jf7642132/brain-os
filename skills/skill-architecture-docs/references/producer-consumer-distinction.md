# 生产者 vs 消费者 区分指南

## 核心区分

| 维度 | 生产者（Producer） | 消费者（Consumer） |
|------|-------------------|-------------------|
| **本质** | 真实可运行的技能 | Agent 使用知识的抽象方式 |
| **可运行性** | 可通过 `hermes skills run` 执行 | 不是技能，无法独立运行 |
| **输出** | 写入知识存储 | 从知识存储读取 |
| **示例** | `chronicle-agent`, `observer` | 知识检索、任务执行、决策支持 |

## 为什么消费者不是技能

消费者描述的是 **Agent 如何使用知识**，而不是独立的可运行组件：

| 消费者 | 实际对应 |
|--------|----------|
| 知识检索 | `memory` 工具、`session_search`、知识库搜索 |
| 任务执行 | `todo` 工具、`kanban` 工具 |
| 决策支持 | 模型本身的推理能力 |
| 技能调用 | `skill_view`、`skill_manage` |
| 上下文管理 | 对话上下文、`memory` 工具 |

这些是 **Agent 的内置能力**，不是需要单独安装的技能。

## 文档编写规则

### ✅ 正确写法

**生产者表格**（列出真实技能）：

```markdown
| 技能 | 定位 | 触发方式 |
|------|------|----------|
| `chronicle-agent` | 对话记录归档 | 夜间定时 |
| `observer` | Agent 自检 | 每次任务完成后 |
```

**消费者说明**（作为概念分类）：

```markdown
### 消费者（5 个）

消费者不是独立技能，而是 Agent 使用知识的抽象方式：

| 消费者 | 输入 | 作用 |
|--------|------|------|
| 知识检索 | `knowledge/04-queries/` | 快速检索知识 |
| 任务执行 | `knowledge/06-context/todo-tracking/` | 执行待办任务 |

> **注意**：消费者是概念分类，不是独立可运行的技能。它们对应 Agent 的内置能力（如 `memory`、`todo` 等）。
```

### ❌ 错误写法

将消费者写成技能列表：

```markdown
| 消费者 | 输入 | 作用 |
|--------|------|------|
| 知识检索 | `knowledge/04-queries/` | 快速检索知识 |
| 任务执行 | `knowledge/06-context/todo-tracking/` | 执行待办任务 |
| 决策支持 | `knowledge/02-concepts/` | 基于知识做决策 |
| 技能调用 | `knowledge/07-skills/` | 调用自定义技能 |
| 上下文管理 | `knowledge/06-context/` | 管理对话上下文 |
```

这样写会让用户误以为这些是需要单独安装的技能。

## 检查清单

文档完成后检查：

- [ ] 生产者列表中的技能名是否真实存在
- [ ] 消费者是否标注"不是独立技能"
- [ ] 消费者是否对应 Agent 的内置能力
- [ ] 技能名与实际 cron 配置一致