# Observer Daily Iteration Plan — {{DATE}}

> Auto-generated by Observer cron, published to #observer channel
> Full learnings ledger: `.learnings/observer/`

---

## 📊 Today's Execution Overview

| Metric | Value |
|--------|-------|
| Total sessions | {{SESSION_COUNT}} |
| Completed successfully | {{SUCCESS_COUNT}} ({{SUCCESS_RATE}}%) |
| Timed out / failed | {{FAIL_COUNT}} ({{FAIL_RATE}}%) |
| Gateway errors total | {{GW_ERROR_COUNT}} |
| Fallback triggered | {{FALLBACK_COUNT}} |

---

## 🔴 Infrastructure Anomalies

> Infrastructure layer: Gateway log analysis

- **[{{type}}]** {{message}} — occurred {{count}} times {{#if is_recurring}}⚠️ recurring{{/if}}
- ✅ No anomalies (if none)

---

## 🟠 Agent Execution Anomalies

> Agent layer: session analysis

- **[{{severity}}][{{recurrence}}x] {{agent}} — {{error_type}}**
  - Trigger context: {{context}}
  - Sessions: {{session_ids}}
  - Written to: `.learnings/observer/errors/{{date}}-{{slug}}.md`
- ✅ No anomalies (if none)

---

## 💡 Suggested Changes Today

### 1. {{title}}
- **Type**: prompt / skill / model / cron / config
- **Trigger**: {{trigger}}
- **Suggested action**: {{action}}
- **Expected effect**: {{expected}}
- **Risk level**: low / medium / high

---

## 🚫 Not Changing Today

### 1. {{title}}
- **Reason**: {{reason}}
- **Revisit when**: {{condition}}

---

## 🔑 Needs Human Decision

| Item | Context | Options |
|------|---------|---------|
| {{item}} | {{context}} | A: ... / B: ... |

---

## 📁 Files Updated Today

- `.learnings/observer/plans/{{DATE}}-iteration-plan.md` (this file)
- `.learnings/observer/index.json` (recurrence counts updated)
- `.learnings/observer/raw/{{DATE}}-sessions.jsonl`
- `.learnings/observer/raw/{{DATE}}-gateway.jsonl`
- **`~/.hermes/todo-backlog.md`** (结构化待办已追加，Step 7)
- New learnings: (list if any)

---

## 📋 结构化待办输出（自动写入 todo-backlog.md）

> 此行及以下内容由 Step 7 自动处理，写入 todo-backlog.md 后删除此区块

### 问题/发现/待办

{{#each suggested_changes}}
- [obs-pending] {{priority_emoji}} **{{title}}** ({{type}}, {{status}})
  - 描述：{{description}}
  - 风险：{{risk_level}}
{{/each}}

{{#each needs_decision}}
- [obs-pending] 🟡 **{{item}}** (decision, pending_confirm)
  - 描述：{{context}}
  - 选项：A: {{options.A}} / B: {{options.B}}
{{/each}}

### 执行写入

```bash
python3 ~/.hermes/scripts/write-todo-from-output.py \
  --output .learnings/observer/plans/{{DATE}}-iteration-plan.md \
  --source observer-daily \
  --format detail
```
